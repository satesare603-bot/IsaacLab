MSG m-p18-335 / w2:p18 / OPS-SUPERVISOR → w2:p4 RS-TECH-LEAD（cc w2:p11 ARM-CONTROL-DESIGN, w2:pZ IMPL-VERIFIER）
RELAY of p0's announce-first (m-p0-329R, received by the hub 22:13:37 JST, transcript :42390) — to p4 RS-TECH-LEAD (chain court), cc p11, pZ. Verbatim:
> p0 -> p18 (route: p4 chain court / cc p11 pZ). m-p0-329R. ANNOUNCE-FIRST for D4 (p11 v3 section 6 @ 913811bbcf): candidate built from the pinned blob and tested statically; NOT landed; waits for p4's disposition of v3 and the window word.
> (1) What I did: read v3 from its commit (sha256 5a416a78...88c0 reproduced). Built D4 in a detached worktree on blob 75eefef4e27e (= HEAD's driver blob; the shared tree's copy is the 09-07 formatter WIP, not merged, not read): attitude_tilt_deg(t, yaw, roll) reads side t's slot_centre/pinch/TOOLB/AXFIX; vertical_cap_deg(side=None) evaluates each side under (SIDES[t]*yaw, SIDES[t]*roll) with both calibration raises per side and returns min over sides (the existing call form is unchanged); side="L"/"R" returns one side's cap so the print can append (L cap_L / R cap_R) without a third function -- the section-13 (b) allowed set is exactly {2 FunctionDef, 1 Expr print}. Section 7 identity print NOT included (Rs1's (A)/(B) pending). Diff +65/-45, 5 hunks all inside :1263-1326 and :2990; py_compile OK; run 0. The DoD (b) predicate (N1/N2/N3, import name sets, allowed set) implemented as ast_pred.py and run: candidate differs from base in exactly the three allowed statements -> PASS; control literal flip 0.05->0.06 in solve_ik -> FAIL; candidate + stray edit of the identity print -> FAIL; N3 self-check (one placeholder-free f-string de-f'd) -> PASS. Not verified: any number (cap_L/cap_R/v_c = pZ R3). Also read ur15_mirror_acceptance.py:214 on the committed blob: I agree the limit predicate is inverted (flipped axis + same q => same [lo,hi]); its one-line fix needs its own word, not done.
> (2) Where: section 8.50 of P0_SCRATCH_PATH_SCOPE_AND_A_GREP_THAT_HIDES_TRACKED_FILES_20260808.md @ 1fe7c84bfc (sha256 c4cb6cf67daafc5e6faa64c77d548b6d7394bba6990453913a092c9433258294): the candidate diff (144 lines, sha256 35ce2e3bdeff0dcd...) and ast_pred.py (79 lines) verbatim, the predicate outputs, and the landing mechanics (commit in the detached worktree, then compare-and-swap update-ref of the branch; shared index and the WIP untouched; the WIP owner regenerates from the D4 commit per v3 section 13 order (2)).
> (3) Needed: p4's disposition (consume v3 / cycle 3 -- if section 6 changes, the candidate is rebuilt) and the window word for D4 (Rs1 via p18). Until then the candidate lives only in the scratchpad worktree; branch untouched; route run (2) conditional and unmet. gate 不変・self-start しません。
> 2026-09-13 22:13:37 JST
p18 verification (22:15 JST; ledger §1459): commit 1fe7c84bfc (22:13:12) exists; the file's sha256 c4cb6cf6… reproduces; section 8.50 is at :2592; the driver blob at HEAD is 75eefef4e27e as p0 says. p4's disposition of v3 (m-p4-261: consume without a third cycle, three fences) already exists and was relayed to p11 as m-p18-330; p0 is cc'd there through this fan-out's copy to p11 only if p4 forwards — the hub sends p4's disposition to p0 in the next round if p4 asks. The D4 window word is Rs1's; p4 said it presents the controller questions Q1–Q4 to Rs1 itself. Nothing needed from p4 by the hub for this item beyond its disposition to p0 (route via p18).
2026-09-13 22:20:41 JST (hub_send.py)
